# LiveSplit.CounterSplits
A LiveSplit Component that displays a list of split times and deltas in relation to a comparison.

I've added functionality to the PB save key, so it doesn't overwrite any values blindly, and only updates the values if the split has a lower value than the current PB. 
